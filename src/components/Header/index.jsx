import React, { PureComponent } from 'react';
import './style.scss';

export default class Header extends PureComponent {
    render() {
        return (
            <header>
                <h1>Shopping</h1>
            </header>
        );
    }
}
